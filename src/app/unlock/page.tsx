import { UnlockForm } from "@/components/unlock/unlock-form";

export const metadata = {
  title: "Enter",
  robots: { index: false, follow: false },
};

export default function UnlockPage() {
  return <UnlockForm />;
}
